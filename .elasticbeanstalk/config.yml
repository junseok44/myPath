branch-defaults:
  deploy:
    environment: MyPath3-env
  main:
    environment: MyPath3-env
  master:
    environment: MyPath3-env
global:
  application_name: mypath3
  default_platform: Docker running on 64bit Amazon Linux 2
  default_region: ap-northeast-2
  sc: git
